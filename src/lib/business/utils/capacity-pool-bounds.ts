// The ceiling of the two Day Setup pool fields. Not beside `BUDGET_BOUNDS`
// because the fitted-pool offer in `business/model` stops at the field's range
// too (R1), and `utils/` is what a component may value-import.
export const CAPACITY_POOL_MAX_HOURS = 16;
